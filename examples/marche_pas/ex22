%PROGRAM% 

type type_tableau : array[1..y,1..3] of int;

type type_normal : struct
  test_int : int;
  test_char : char;
end struct;

start {
  var x : entier; # Ne fonctionne pas car les déclarations doivent être avant le bloc d'instruction
}
