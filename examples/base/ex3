%PROGRAM3%

proc proc_1 (arg:int, arg2:char) 
var coucou : char;

start {
  if (x == 6) then {
    x = 3;
  }
  else if (y == 4) then {
    write("Héhé");
  }
  else {
    write("Pas de bol");
  }
};

function pipi (arg:int, arg2:string[10]) return int 
var coucou : char;
    
start {
  return 1;
};

// Mon premier commentaire
# Mon deuxième commentaire
-- C'est mon dernier commentaire --
start{x=3;} /* Ceci est mon dernier commentaire
pour dire "/* lol */" qu'on est obligé de mettre 
x = 3; */
